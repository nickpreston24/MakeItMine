import { MenuContainer, Layout, Login } from '../components';

const Index = () => {
    return (
        <div>
            <Layout />
            <MenuContainer />
            <Login />
        </div>
    )
};

export default Index;